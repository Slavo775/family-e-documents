generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Stub model required for prisma generate — remove when first real model is added in change 02+
model Placeholder {
  id Int @id @default(autoincrement())

  @@map("_placeholder")
}
