export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum FolderAction {
  VIEW = 'VIEW',
  UPLOAD = 'UPLOAD',
  DELETE = 'DELETE',
  MANAGE = 'MANAGE',
}

export enum VisibilityMode {
  PUBLIC = 'PUBLIC',
  RESTRICTED = 'RESTRICTED',
}

export enum DocumentStatus {
  PENDING = 'PENDING',
  ACTIVE = 'ACTIVE',
  DELETED = 'DELETED',
}
